@page
@model WebAppTest.Pages.TestModel
@{
}

@if (Model.errorMessage.Length > 0)
{
    <div>
        <strong>@Model.errorMessage</strong>
    </div>
}


<div>


    <table witdh="80%"> 

        <tr>
            <th>Nome</th>
            <th>Cognome</th>
            <th>DataNascita</th>
            <th>Telefono</th>

      </tr>

        @foreach(var x in Model.lista)
        {
            
            <tr>
                <td width="20"> @x.Nome</td>
                <td width="20%"> @x.Cognome</td>
                <td width="20%"> @x.DataNascita</td>
                <td width="20%"> @x.Telefono</td>
                <td>
                 <a class="btn btn-primary btn-sm" href="modifica?id=@x.id">Modifica</a>
                 <a class="btn btn-danger btn-sm"  href="cancella?=@x.id">Cancella</a>
                </td>
            </tr>
      
        }
     </table>
 </div>